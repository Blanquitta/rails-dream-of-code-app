<%= form_with(model: course) do |form| %>
  <% if course.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :coding_class_id, style: "display: block" %>
    <%= form.select :coding_class_id, options_from_collection_for_select(@courses, :id, :title), { prompt: 'Select Lesson' }, { class: 'form-control' } %>
  <% </div> %> 
  
  <%= form.select :coding_class_id,
      options_from_collection_for_select(@courses, :id, :title),
      { prompt: 'Select Lesson' },
      { class: 'form-control' } %>
   
  </div>

  <div>
    <%= form.label :trimester_id, style: "display: block" %>
    <%= form.text_field :trimester_id %>
  </div>

  <div>
    <%= form.label :max_enrollment, style: "display: block" %>
    <%= form.number_field :max_enrollment %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
